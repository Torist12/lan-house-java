================================================================================
  TODO — lan-house-java
  Base: commit 8c81665 (Merge branch 'dev')
  Generated on: 2026-05-20
================================================================================
  Priority Legend:        [!] critical / blocks others
                          [~] important / recommended
                          [-] improvement / optional
================================================================================


──────────────────────────────────────────────────────────────────────────────
  BLOCK 1 — REPOSITORY INTERFACE INTRODUCTION
  (central refactor; unblocks everything below)
──────────────────────────────────────────────────────────────────────────────

  [x] 1.1  Create IClienteRepositorio interface
           Suggested package: com.lanhouse.repository
           Methods: salvar(), listarTodos(), buscarPorId(),
                    buscarPorDocumento(), atualizar(), deletar()

  [x] 1.2  Create IComputadorRepositorio interface
           Methods: listarTodos(), buscarPorId(), buscarPorNumero(),
                    listarLivres(), atualizarStatus()

  [x] 1.3  Create ILocacaoRepositorio interface
           Methods: iniciarLocacao(), finalizarLocacao(), listarAtivas(),
                    listarPorCliente(), listarTodas(), buscarPorId()

  [x] 1.4  Add "implements IClienteRepositorio" to ClienteDAO
           No method changes — declaration only.

  [x] 1.5  Add "implements IComputadorRepositorio" to ComputadorDAO
           No method changes.

  [x] 1.6  Add "implements ILocacaoRepositorio" to LocacaoDAO
           No method changes.

  [x] 1.7  Add "implements IClienteRepositorio" to ClienteDAOMemoria
           No method changes — already has the correct methods.

  [x] 1.8  Add "implements IComputadorRepositorio" to ComputadorDAOMemoria
           No method changes.

  [x] 1.9  Add "implements ILocacaoRepositorio" to LocacaoDAOMemoria
           No method changes.

  [x] 1.10 Update AppService constructor to receive interfaces:
           AppService(IComputadorRepositorio, IClienteRepositorio,
                      ILocacaoRepositorio, CalculoValorService)
           Method body does not change — only the signature.

  [x] 1.11 Update Main to pass concrete implementations
           (still passing ClienteDAO etc., but now via interface)


──────────────────────────────────────────────────────────────────────────────
  BLOCK 2 — DAO MODULE CLEANUP
  (after block 1 completed)
──────────────────────────────────────────────────────────────────────────────

  [x] 2.1  Move ClienteDAO, ComputadorDAO, LocacaoDAO to
           com.lanhouse.repository.sqlite
           (separates implementation from contract)

  [x] 2.2  Move ClienteDAOMemoria, ComputadorDAOMemoria, LocacaoDAOMemoria
           to com.lanhouse.repository.memory

  [x] 2.3  Move DatabaseConnection to com.lanhouse.repository.sqlite
           (it's an SQLite implementation detail, not a root dao package detail)

  [x] 2.4  Rename dao/ package to repository/ throughout the project
            (naming more aligned with the adopted pattern)

  [x] 2.5  Isolate "SELECT last_insert_rowid()" into a private utility method
           within each SQLite DAO that uses it (ClienteDAO, LocacaoDAO).
           Reduces duplication and facilitates future SQL dialect changes.


──────────────────────────────────────────────────────────────────────────────
  BLOCK 3 — DATABASECONNECTION AND SCHEMA MANAGEMENT
  (independent of block 1, can be done in parallel)
──────────────────────────────────────────────────────────────────────────────

  [x] 3.1  Separate criarTabelas() from DatabaseConnection into its own class
            (e.g., SchemaManager or DatabaseMigrator).
           DatabaseConnection should only do one thing: provide a Connection.

  [x] 3.2  Remove call to DatabaseConnection.criarTabelas() from Main.
           Schema initialization is not the responsibility of the entry point.
           Move to SchemaManager called internally by the SQLite impl
           of the repository, or in a separate bootstrap method.

  [-] 3.3  Introduce a minimal connection pool (e.g., reuse via a singleton
            Connection instance for SQLite single-file).
            Currently, each DAO method opens and closes a new Connection.

  [x] 3.4  Add explicit FOREIGN KEY REFERENCES in the DDL of locacoes.
            Currently, referential integrity between locacoes → clientes and
            locacoes → computadores is not enforced by the database.
            Add: FOREIGN KEY (cliente_id) REFERENCES clientes(id)
                       FOREIGN KEY (computador_id) REFERENCES computadores(id)
            And enable: PRAGMA foreign_keys = ON


──────────────────────────────────────────────────────────────────────────────
  BLOCK 4 — DOMAIN MODEL (RICH MODEL)
──────────────────────────────────────────────────────────────────────────────

  [~] 4.1  Add document validation to Cliente
            E.g., validateDocument() checking basic CPF/CNPJ format.
            Throws IllegalArgumentException in the constructor if invalid.
            (Note: Basic validation is in AppService, but not yet in the model constructor itself).

  [x] 4.2  Transform Computador status into an enum
            enum StatusComputador { LIVRE, OCUPADO }
            Eliminates "magic strings" "livre"/"ocupado" scattered in
            AppService, ComputadorDAO, and LocacaoDAOMemoria.

  [x] 4.3  Transform Computador tier into an enum
            enum TierComputador { BASICO, INTERMEDIARIO, GAMER }
            Eliminates "magic strings" in CalculoValorService and DAOs.
            The switch in getTaxaTier() becomes exhaustive and no default is needed.

  [x] 4.4  Transform Locacao status into an enum
            enum StatusLocacao { ATIVA, FINALIZADA }
            Eliminates "magic strings" "ativa"/"finalizada".

  [x] 4.5  Move getTempoMinutos() and getTempoHoras() from Locacao for calculation
            within CalculoValorService, or keep in Locacao but ensure
            they are only called after fim != null (add guard clause).


──────────────────────────────────────────────────────────────────────────────
  BLOCK 5 — QUALITY AND ROBUSTNESS
──────────────────────────────────────────────────────────────────────────────

  [~] 5.1  Add try-with-resources in Main (or remove Connection from Main)
           Currently, Main does not close JDBC resources if an exception occurs
           before controller.executar() returns. (Partially addressed by AppController's
           Scanner closing and DatabaseConnection's error handling).

  [x] 5.2  Replace System.err.println in DAOs with a consistent error strategy:
           either rethrow as RuntimeException, or return Optional<T> instead of null.
           Silently returning null masks database failures.

  [x] 5.3  Close Scanner in CliView when closing the application.
            Currently, the System.in Scanner is not closed. Add close() or
            use try-with-resources in the main loop of AppController.

  [x] 5.4  Handle NumberFormatException in lerOpcaoMenu() so that it does not
            return -1 without informing the user. Currently, it returns -1 and the switch
            falls into the default displaying "Invalid option!" — acceptable, but could
            loop asking for new input.


──────────────────────────────────────────────────────────────────────────────
  BLOCK 6 — TESTS
  (only becomes viable after block 1 — repositories via interface)
──────────────────────────────────────────────────────────────────────────────

  [ ] 6.1  Write unit tests for CalculoValorService
           Does not depend on any interface, already testable now.
           Cover: BASICO, INTERMEDIARIO, GAMER, zero time, fractional time.

  [ ] 6.2  Write unit tests for AppService using DAOMemoria
           After block 1: AppService(IClienteRepositorio...) can receive
           ClienteDAOMemoria without touching the database. Test the complete flow of
           iniciarLocacao() and finalizarLocacao() without SQLite.

  [ ] 6.3  Write integration tests for SQLite DAOs
           Using a temporary in-memory SQLite database
           (jdbc:sqlite::memory:) to avoid polluting the development database.

  [ ] 6.4  Create an IView implementation for testing (FakeView / StubView)
           Allows testing AppController without depending on Scanner or console.


──────────────────────────────────────────────────────────────────────────────
  BLOCK 7 — MISSING DOMAIN FUNCTIONALITIES
──────────────────────────────────────────────────────────────────────────────

  [x] 7.1  CRUD for computers via controller/service
            Currently, computers are only created by database bootstrap.
            Add: cadastrarComputador(), removerComputador(),
            alterarTier(), alterarPreco() — with corresponding methods
            in repositories and menus in IView.

  [ ] 7.2  Revenue report by period
            List finalized rentals within a date range and sum
            valor_total. Requires date filter in ILocacaoRepositorio.

  [x] 7.3  Duplicate rental validation
            Prevent the same client from opening two active rentals at the same
            time. AppService.iniciarLocacao() must check if there is an active rental
            for the clientId before creating a new one.

  [ ] 7.4  Display elapsed time in active rentals
            listarLocacoesAtivas() could display how long each
            rental has been open (LocalDateTime.now() - locacao.getInicio()).


================================================================================

  SUGGESTED EXECUTION ORDER:

  Sprint 1 (unblocker): Block 1 complete → Block 2.1–2.3
  Sprint 2 (quality):     Block 3 + Block 5 + Block 4 (enums first)
  Sprint 3 (tests):        Block 6 (from inside out: unit → integration)
  Sprint 4 (product):       Block 7 (business functionalities)

================================================================================
  END OF TODO
================================================================================
